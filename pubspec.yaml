name: weather_app
description: App de clima con diseño exacto
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0  # Para League Spartan

flutter:
  uses-material-design: true
  assets:
    - assets/